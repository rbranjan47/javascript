//using strict mode, to write secure code to avoid from the errors
'use strict';
// x=3.14; Strict mode will give error as it is not declared
 let hasInverter = false;
 const haveMoney = true;
 
 if (haveMoney) hasInverter = true;
 if(hasInverter)
 {
     console.log("ready to buy inverter");
 }

 const x=3.14;

 
       
